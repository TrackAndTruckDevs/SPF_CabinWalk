#pragma once
#include <cstdint>

namespace SPF_CabinWalk::Animation::Config
{
    // Default animation durations in milliseconds.
    // These can be overridden by user settings in the future.

    constexpr uint64_t DRIVER_TO_PASSENGER_DURATION_MS = 3500000;
    constexpr uint64_t PASSENGER_TO_DRIVER_DURATION_MS = 3500000;

} // namespace SPF_CabinWalk::Animation::Config
